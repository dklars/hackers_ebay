{% extends 'base.html.twig' %}

{% block title %}LinkType{% endblock %}

{% block body %}
    <h1>LinkType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ link_type.id }}</td>
            </tr>
            <tr>
                <th>NameLink</th>
                <td>{{ link_type.nameLink }}</td>
            </tr>
            <tr>
                <th>DomaineLink</th>
                <td>{{ link_type.domaineLink }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_link_type_index') }}">back to list</a>

    <a href="{{ path('app_link_type_edit', {'id': link_type.id}) }}">edit</a>

    {{ include('link_type/_delete_form.html.twig') }}
{% endblock %}
